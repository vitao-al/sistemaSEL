# Dependências do projeto (mapeadas do package.json)
# Observação: este projeto é Next.js/Node.js (npm), não Python.
# Use `npm install` para instalar as dependências.

# ── Runtime ─────────────────────────────────────────────────────────────
next==14.2.5
react>=18,<19
react-dom>=18,<19

# UI e componentes
recharts>=2.12.7,<3
lucide-react>=0.408.0,<1
clsx>=2.1.1,<3

# Formulários e validação
react-hook-form>=7.52.1,<8
zod>=3.23.8,<4
@hookform/resolvers>=3.9.0,<4

# Estado global
zustand>=4.5.4,<5

# Datas
date-fns>=3.6.0,<4

# Autenticação / JWT
jsonwebtoken>=9.0.3,<10

# Email / recuperação de senha
nodemailer>=8.0.3,<9
resend>=6.9.4,<7

# Relatórios / exportações
jspdf>=4.2.1,<5
jspdf-autotable>=5.0.7,<6

# Banco de dados
@prisma/client>=5.22.0,<6

# ── Dev / Build ──────────────────────────────────────────────────────────
typescript>=5,<6
@types/node>=20,<21
@types/react>=18,<19
@types/react-dom>=18,<19
@types/jsonwebtoken>=9.0.10,<10
@types/nodemailer>=7.0.11,<8
prisma>=5.22.0,<6
eslint>=8,<9
eslint-config-next==14.2.5
vitest>=3.2.4,<4
